<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 被选中时，颜色变为白色 -->
    <item android:state_checked="true" android:color="@color/tv_color"/>
    <!-- 没有被选中时，颜色变为灰色 -->
    <!-- <item android:state_checked="false" android:color="#00f"/>如果这里设置，那么按下功能就没有作用了 -->
    <!-- 按下是显示 -->
    <item android:state_pressed="true" android:color="@color/blue_main"/>
    <!-- 松开时颜色变为 -->
    <item android:state_pressed="false" android:color="#000"/>

</selector>